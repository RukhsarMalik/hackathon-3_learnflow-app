apiVersion: v1
kind: Service
metadata:
  name: triage-service
  namespace: default
spec:
  selector:
    app: triage-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
