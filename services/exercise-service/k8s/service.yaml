apiVersion: v1
kind: Service
metadata:
  name: exercise-service
  namespace: default
spec:
  selector:
    app: exercise-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
