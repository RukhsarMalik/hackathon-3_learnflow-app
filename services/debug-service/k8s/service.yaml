apiVersion: v1
kind: Service
metadata:
  name: debug-service
  namespace: default
spec:
  selector:
    app: debug-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
