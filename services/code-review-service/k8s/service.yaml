apiVersion: v1
kind: Service
metadata:
  name: code-review-service
  namespace: default
spec:
  selector:
    app: code-review-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
