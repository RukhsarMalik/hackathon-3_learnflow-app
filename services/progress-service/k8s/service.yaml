apiVersion: v1
kind: Service
metadata:
  name: progress-service
  namespace: default
spec:
  selector:
    app: progress-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
