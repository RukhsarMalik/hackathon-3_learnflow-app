apiVersion: apps/v1
kind: Deployment
metadata:
  name: concepts-service
  namespace: default
  labels:
    app: concepts-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: concepts-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: concepts-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "concepts-service"
        dapr.io/app-port: "8000"
    spec:
      containers:
      - name: concepts-service
        image: concepts-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        env:
        - name: SERVICE_NAME
          value: "concepts-service"
