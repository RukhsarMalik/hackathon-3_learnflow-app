apiVersion: v1
kind: Service
metadata:
  name: concepts-service
  namespace: default
spec:
  selector:
    app: concepts-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
